{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "messenger",
  "password": "messenger",
  "database": "messenger",
  "synchronize": true,
  "logging": false,
  "entities": [
    "./src/entities/**/*.ts"
  ],
  "migrations": [
    "./src/migration/**/*.ts"
  ],
  "subscribers": [
    "./src/subscriber/**/*.ts"
  ]
}
